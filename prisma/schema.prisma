
generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
    url = env("POSTGRES_URL")
}

model Pokemon {
  id	Int	@id	@default(autoincrement())
  name	String	@unique
  type	String
  sprite String?
}
